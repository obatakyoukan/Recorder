
<% if current_user %>
  <div>
  <ul class="account-menu" >
    <%= menu_link_to "ようこそ"+current_user.name+"さん!" , :account %>
    <%= menu_link_to "ログアウト", :session,
      method: :delete, data: { confirm: "ログアウトしますか?" } %>
  </ul>
  </div>
<% end %>

<h1> Recorder app </h1>

<nav class="menubar">
  <ul>
    <%= menu_link_to "TOP" , :root %>
    <% if current_user %>
     <%= menu_link_to "記録一覧", :books %>
    <% else %>
     <%= menu_link_to "新規登録", :users %>
    <% end %>
  </ul>
</nav>
